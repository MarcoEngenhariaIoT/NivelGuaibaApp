# NivelGuaibaApp

##App que mostra o nível do rio guaíba - Versão - Trabalho de extensão da faculdade Estácio - Cadeira Programacao Para Dispositivos Moveis em Android da graduação de Engenharia de Software.

```
npx create-expo-app --template
```

### What is your app named? ... NivelGuaibaApp

```
npm install react-dom@19.1.0 --legacy-peer-deps

npm install react-native-web@^0.21.0 --legacy-peer-deps
```

### instalar se estiver diferente do react-dom@19.1.0, tem que está na mesma versão.

```
npm install reac@19.1.0 --legacy-peer-deps
npm install @types/react@~19.1.10 --legacy-peer-deps
npm install typescript@~5.9.2 --legacy-peer-deps
npm start
```
